import { StyleSheet, Text, View, Pressable } from "react-native";

export default function TrackFood() {
  return <View style={styles.page}></View>;
}

const styles = StyleSheet.create({
  page: { flex: 1, backgroundColor: "#f7f5fb" },
});
